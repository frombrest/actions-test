#-------------------------------------------------
# Master pipeline
# test code => create release => deploy to env
#
#-------------------------------------------------

name: master-pipeline

on:
  push:
    branches:
      - master

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        run : |
          echo "One"
          echo "Two"
          echo "Three"